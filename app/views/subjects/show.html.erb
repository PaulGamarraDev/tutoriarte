<h2><%= @subject.name %></h2>

Grado: <%= @subject.grade_subject %>

<br>

Descripción: <%= @subject.description %>

<br>
<%= link_to "Edit", edit_subject_path(@subject)%>

<%= link_to "Delete",
  subject_path(@subject),
  data: {turbo_method: :delete, turbo_confirm: "¿Seguro que quieres borrar la materia?"}%>

<%= link_to "Regresar al inicio", subjects_path %>
